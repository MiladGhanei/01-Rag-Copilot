# Daily Report — 2025-11-03
- **Time spent:** ~45 minutes
- **What I set up:** Git repo; Python 3.11 venv; core deps installed; project skeleton (`app/...`, `data`, `models`, `scripts`, `tests`, `docker`).
- **Code/modules touched:** 
  - `app/core/config.py` (DOCS_DIR, STORE, INDEX, EMB_MODEL)
  - `app/retrieval/ingest.py` (PDF → JSONL chunks)
  - `app/retrieval/indexer.py` (embeddings → FAISS index)
- **Commands run:** 
  - `python3.11 -m venv .venv && source .venv/bin/activate`
  - `pip install fastapi uvicorn[standard] streamlit pypdf sentence-transformers faiss-cpu transformers peft datasets bitsandbytes pydantic`
  - `mkdir -p app/{core,retrieval,generation,guardrails,api,ui,eval} data models scripts tests docker`
  - `touch app/core/config.py app/api/main.py app/ui/app.py README.md .gitignore`
  - Ingest/index: `python -c "from app.retrieval.ingest import run; run()"` and `python -c "from app.retrieval.indexer import run; run()"`
- **Concepts learned:** venv basics; package roles; FAISS for fast semantic nearest-neighbor search; ingest/index pipelines.
- **Blockers:** none today.
- **Next:** minimal `/ask` API + simple UI to return cited answers.